
  # School Transport Hub Redesign

  This is a code bundle for School Transport Hub Redesign. The original project is available at https://www.figma.com/design/TcdRkhFd0dr3NV1ps73Ge6/School-Transport-Hub-Redesign.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  